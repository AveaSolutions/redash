# Dependencies of the extension/bundle loading mechanism
# (redash/extensions.py and bin/bundle-extensions).
# It's automatically installed when running npm run bundle.
#
# importlib-metadata is pinned to 4.13.0: redash/extensions.py and
# bin/bundle-extensions use the legacy dict-style entry_points() API,
# which was removed in importlib-metadata 5.0.
importlib-metadata==4.13.0
importlib_resources==5.13.0
