atsd_client==3.0.5
azure-kusto-data==0.0.35
boto3==1.28.8
botocore==1.31.8
cassandra-driver==3.21.0
# certifi is needed to support MongoDB and SSL:
certifi>=2023.7.22
cmem-cmempy==21.2.3
# dql/dynamo3 (DynamoDB SQL runner) are installed separately with --no-deps in the
# Dockerfile: dql 0.5.x declares python-dateutil<2.7.0 which is unresolvable next to
# botocore/azure-kusto, but it works fine at runtime with python-dateutil 2.8.
# (dql 0.6.x is NOT import-compatible with pyparsing>=2.2 and would crash query runner
# imports with an AttributeError, so do not "upgrade" it.)
# dql==0.5.26
# dynamo3==0.4.10
# Pin: 1.0.3+ requires firebolt-sdk>=1.5.0; keep the version the fork has been running.
firebolt-sqlalchemy==1.0.2
google-api-python-client==1.7.11
google-auth>=2.22.0
gspread==3.1.0
# ibm_db ships a prebuilt DB2 client driver for x86_64 only.
ibm-db>=2.0.9; platform_machine == "x86_64"
impyla==0.16.0
influxdb==5.2.3
memsql==3.2.0
mysqlclient==2.1.1
oauth2client==4.1.3
# numpy 2.x is ABI-incompatible with pandas 1.3.x wheels
numpy<2
openpyxl==3.0.7
# pandas is needed by the excel/csv query runners (previously pulled in
# transitively by pymapd, which has been dropped)
pandas==1.3.4
phoenixdb==0.7
protobuf==3.18.3
PyAthena>=1.5.0,<=1.11.5
pydgraph==2.0.2
pydruid==0.5.7
pyexasol==0.12.0
pyhive==0.6.1
# pymapd was dropped: its final release (0.19.0) hard-pins thrift==0.11.0, which is
# unresolvable next to impyla/thrift 0.16.0 under the modern pip resolver. The mapd
# query runner degrades gracefully to disabled when the import fails.
pymongo[tls,srv]==4.6.3
# 2.2.11 (same 2.2.x line) is the first release with prebuilt aarch64 wheels;
# 2.2.8's source build fails under modern Cython.
pymssql==2.2.11
pyodbc==4.0.28
python-rapidjson==1.1.0
qds-sdk>=1.9.6
requests_aws_sign==0.1.5
sasl>=0.1.3
simple_salesforce==0.74.3
snowflake-connector-python==3.18.0
td-client==1.0.0
thrift==0.16.0
thrift_sasl>=0.1.0
# Capped at 0.334: trino 0.335+ requires requests>=2.32.4, but requests is pinned to
# 2.31.0 (advocate's SSRF protection hooks break under requests 2.32's new
# get_connection_with_tls_context code path).
trino>=0.305,<=0.334.0
vertica-python==1.1.1
xlrd==2.0.1
